import { Credentials } from "types/credentials"

/**
 * Create auth handler and register asymetric keys
 * @param credentials generated private and public key
 * @returns Functions for key validation and generation
 */
export const createAuthHandler = (credentials: Credentials) => {
    return ({

    })
}